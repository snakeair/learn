// ts of types

export type Todo = {
  id: number;
  name: string;
  bl: boolean;
};

export type titleName = string;

export type user = {
  name: string;
  token: string;
  img?: string;
  sex: boolean;
};
